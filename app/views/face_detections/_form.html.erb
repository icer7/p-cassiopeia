<%= form_with(model: face_detection, local: true) do |form| %>
  <% if face_detection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(face_detection.errors.count, "error") %> prohibited this face_detection from being saved:</h2>

      <ul>
      <% face_detection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
